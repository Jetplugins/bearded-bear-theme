# Corresponding source

The complete corresponding source for Bearded Theme: Dark & Light version `${pluginVersion}` is
available without charge at:

https://github.com/Jetplugins/bearded-bear-theme/tree/${pluginVersion}

The source repository includes the build scripts and instructions needed to generate the plugin
distribution. The repository and the version tag must be publicly accessible whenever this binary is
distributed.
