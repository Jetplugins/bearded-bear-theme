# Bearded Theme Marketplace Demo

This small project is opened automatically by the repository's `./gradlew createScreenshots` task.
It gives IntelliJ real Java, Markdown, and project-tree content to render
while the installed Bearded Theme plugin cycles through every registered theme.
